[database]
host = %(db_host)s
port = %(db_port)s
database = %(db_name)s
db_user = %(db_user)s
password = %(db_password)s

[secrets]
signature = %(signature_secret)s
jwt = %(jwt_secret)s
system_user_access_token = %(system_user_access_token)s

[sumukha]
app_id = %(sumukha_app_id)s
app_secret = %(sumukha_app_secret)s
business_id = %(sumukha_business_id)s

[facebook]
graph_url_base = %(graph_url_base)s
graph_url_version = %(graph_url_version)s
hub_verify_token = %(hub_verify_token)s
phone_number_id = %(phone_number_id)s

[groq]
groq_secret_key = %(groq_secret_key)

